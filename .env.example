PORT=
JUDGE_PORT=9999 # Port for DMOJ judge server connections
DATABASE_URL= # e.g. postgresql://[user]:[password]@[host]:[port]/[db_name]?schema=public
JWT_CLIENT_TOKEN= # Random with `openssl rand -hex 32`
JWT_JUDGE_TOKEN= # Random with `openssl rand -hex 32`
TURNSTILE_SECRET_KEY= # Cloudflare Turnstile secret key, generated from your site dashboard
BRIDGE_LISTENING_ADDRESS=0.0.0.0 # Address for DMOJ Judge server to listen on